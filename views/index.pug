extends layout
//- laadt de layout.pug in, en vult de block content in met de doctype

block content
  h1= title
  p Welcome to #{title}
  if customer 
    p Welcome #{customer.first_name} #{customer.last_name}.
